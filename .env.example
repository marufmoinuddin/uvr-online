# UVR Local — environment
# Path to the MSST-WebUI checkout used by the GPU worker (primary backend).
MSST_ROOT=/home/maruf/git/MSST-WebUI

# Web → API wiring (browser reaches these via the published ports)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000